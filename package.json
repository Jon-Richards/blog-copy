{
  "name": "blog_copy_2023",
  "version": "0.0.1",
  "description": "A style guide, CSS, and other assets designed to facilitate extended reading.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./scripts/build.js --name='blog-copy'",
    "build:docs": "node ./scripts/build-docs.js --name='blog_copy'",
    "build:docs:watch": "nodemon --watch 'src/**/*' -e js,ts,html,scss ./scripts/build-docs.js --name='blog_copy'",
    "typecheck:watch": "tsc --watch",
    "start": "node ./scripts/server.js"
  },
  "author": "Jon Richards",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.17.2",
    "express": "^4.18.2",
    "nodemon": "^2.0.20",
    "sass": "^1.56.1",
    "typescript": "^4.9.4",
    "yargs": "^17.6.2"
  }
}

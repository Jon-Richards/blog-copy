import type { Section } from '$config';

/*
export function buildRoute(rootSection: Section, endSection: Section) {
  let build = true;
  let section = rootSection;
  let route = rootSection.slug;

  while (build === true) {
    if (Array.isArray(section.subsections) && section.subsections.length) {

    } else {
      build = false;
    }
  }
}
*/
